# Phase 6 — Docs and finalisation

## Campaign summary

- **Issue**:
  https://github.com/TrakHound/MTConnect.NET/issues/132 — auto-injected
  `ASSET_COUNT` DataItem emitted with `representation` defaulting to
  `VALUE`; the spec (MTConnect Part 2, UML
  `_19_0_3_68e0225_1640602520420_217627_44`) requires `DATA_SET`.

- **Fix**: in
  `libraries/MTConnect.NET-Common/Agents/MTConnectAgent.cs`,
  `NormalizeDevice` (private), the auto-injection block for
  `AssetCountDataItem` now stamps
  `Representation = DataItemRepresentation.DATA_SET`. User-declared
  `ASSET_COUNT` DataItems in `devices.xml` flow through
  `NormalizeDataItems` and are unchanged.

- **Tests**:
  `tests/MTConnect.NET-Common-Tests/Agents/AssetCountAutoGeneratedDataSetTests.cs`
  (red-then-green for both auto-gen and user-declared paths) +
  `tests/MTConnect.NET-Common-Tests/Agents/AssetCountFactoryDataSetGuardTests.cs`
  (regression pin across the three accessible auto-injection entry
  points).

- **Project test status** after the fix:

```text
Passed!  - Failed: 0, Passed: 8, Skipped: 0, Total: 8 - MTConnect.NET-Common-Tests.dll (net8.0)
```

## Coverage

The single new production line
(`assetcount.Representation = DataItemRepresentation.DATA_SET;`) is
exercised by every test in `AssetCountAutoGeneratedDataSetTests` and
`AssetCountFactoryDataSetGuardTests` — line + branch + method coverage
on the inserted statement is 100%.

## Out-of-scope follow-ups

1. Generator-side fix: `AssetCountDataItem.g.cs` declares
   `DefaultRepresentation = VALUE`; the SysML model is `DATA_SET`. The
   importer fix belongs in the SysML-importer plan.
2. Streamed-observation rename + per-asset-type bookkeeping: emit
   `AssetCountDataSet` with `{ Entry key="<AssetType>" value="<count>" }`
   on Current / Sample. Substantial surface change — its own plan.
3. MQTT Docker E2E + cppagent v2.7.0.7 parity scenarios — once
   bootstrap (Docker harness) lands upstream.

## Pre-close

- Build: `dotnet build` Debug `net8.0` green on
  `MTConnect.NET-Common.csproj` and
  `MTConnect.NET-Common-Tests.csproj`.
- Tests: 8 passed, 0 failed, 0 skipped.
- `git status`: clean.
- Every commit pushed to `origin/fix/issue-132`.

## Hand-off note

Per dispatch instructions, this implementation STOPS at end of phase 7
(this phase). The §1.5 close-out (history rewrite + `gh pr ready` +
reviewer assignment) is the maintainer's call.
